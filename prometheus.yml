global:
  scrape_interval: 15s

scrape_configs:
  - job_name: warrcloud-api
    metrics_path: /metrics
    static_configs:
      # "api" is the Compose service name, resolvable on the Compose network.
      - targets: ["api:8000"]
